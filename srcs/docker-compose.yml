services:
  nginx:
    image: nginx:inception
    pull_policy: build
    container_name: nginx
    build: requirements/nginx/.
    volumes:
      - wordpress:/var/www/html
    ports:
      - 443:443
    depends_on:
      wordpress:
        condition: service_healthy
    secrets:
      - inception.crt
      - inception.key
    networks:
      - inception
    restart: always

  wordpress:
    image: wordpress:inception
    pull_policy: build
    container_name: wordpress
    build: ./requirements/wordpress/.
    volumes:
      - wordpress:/var/www/html
    healthcheck:
      test: ["CMD", "service", "php8.2-fpm", "status"]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 20s
      start_interval: 1s
    depends_on:
      mariadb:
        condition: service_healthy
    env_file:
      - .env
    secrets:
      - db_password
      - user_password
      - admin_password
    networks:
      - inception
    restart: always

  mariadb:
    image: mariadb:inception
    pull_policy: build
    container_name: mariadb
    build: ./requirements/mariadb/.
    volumes:
      - mariadb:/var/lib/mysql
    healthcheck:
      test: [ "CMD", "mariadb-admin", "ping", "-u", "healthcheck" ]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 20s
      start_interval: 1s
    environment:
      DB_NAME: ${DB_NAME}
      DB_USER: ${DB_USER}
    secrets:
      - db_password
      - db_root_password
    networks:
      - inception
    restart: always

secrets:
  db_password:
    file: /home/${USER}/Inception/secrets/db_password.txt
  db_root_password:
    file: /home/${USER}/Inception/secrets/db_root_password.txt
  admin_password:
    file: /home/${USER}/Inception/secrets/admin_password.txt
  user_password:
    file: /home/${USER}/Inception/secrets/user_password.txt
  inception.crt:
    file: /home/${USER}/Inception/secrets/inception.crt
  inception.key:
    file: /home/${USER}/Inception/secrets/inception.key

volumes:
  wordpress:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/${USER}/data/wordpress
  mariadb:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /home/${USER}/data/mariadb

networks:
  inception:
    driver: bridge
